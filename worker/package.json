{
  "name": "pollpop-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create pollpop",
    "db:migrate:local": "wrangler d1 migrations apply pollpop --local",
    "db:migrate": "wrangler d1 migrations apply pollpop",
    "db:seed:local": "wrangler d1 execute pollpop --local --file=./seed.sql",
    "db:seed": "wrangler d1 execute pollpop --file=./seed.sql",
    "hash-password": "node scripts/hash-password.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260620.0",
    "typescript": "~6.0.3",
    "wrangler": "^4.21.0"
  },
  "dependencies": {
    "jose": "^6.2.3",
    "pollpop": "file:.."
  }
}
